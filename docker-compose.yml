version: '2'

services:
  website:
    build: .
    command: >
      gunicorn -b 0.0.0.0:8000
        --access-logfile -
        "Code.server:create_app()"
    environment:
      PYTHONUNBUFFERED: 'true'
    volumes:
      - '.:/Code'
    ports:
      - '8000:8000'
